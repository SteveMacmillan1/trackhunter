{
  "name": "trackhunter-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd trackhunter && npm install && npx ng build --configuration production --output-path ../nodeserver/spotifinder",
    "start": "node nodeserver/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "dotenv": "^16.0.0"
  }
}